ls -a cd /home unknown
# Неизвестная команда

ls
# Проверяет текущую директорию (пока заглушка)

cd /dir1
# Для deep_vfs, проверка смены директории

ls cd /nonexistent
# Ошибка (пока заглушка)

# Чтобы скрипт не закрывался
# exit
# Выход